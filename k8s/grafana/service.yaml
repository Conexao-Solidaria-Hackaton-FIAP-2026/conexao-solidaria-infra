apiVersion: v1
kind: Service
metadata:
  name: grafana-svc
  namespace: conexao-solidaria
spec:
  selector:
    app: grafana
  type: NodePort
  ports:
    - port: 3000
      targetPort: 3000
      nodePort: 30300
